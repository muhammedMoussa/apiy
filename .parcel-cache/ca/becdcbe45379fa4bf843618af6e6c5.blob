"use strict";

Object.defineProperty(exports, "__esModule", {
  value: true
});
exports.Get = void 0;

/* get function */
const Get = options => {
  return new Promise((resolve, reject) => {
    const xhr = new XMLHttpRequest();
    xhr.open(options.method, options.url);

    try {
      xhr.send();
    } catch (error) {
      reject(new Error("Whoops!"));
    }

    xhr.onload = function () {
      if (xhr.status != 200) {
        reject(new Error("Whoops!")); //   reject(`Error ${xhr.status}: ${xhr.statusText}`);
      } else {
        resolve(xhr.response);
      }
    };
  });
};

exports.Get = Get;